# 漫剧工场 (MangaStudio)

> **AI 一站式短剧 / 漫剧 / 动态漫画生成工作台**
> *An AI-powered workbench that turns story ideas into previewable, exportable video assets.*

漫剧工场是一个面向短剧、漫剧、动态漫画与影视分镜创作者的 AI 生产工作台。它把「故事创意 → 可预览、可导出、可继续剪辑的视觉资产与视频片段」这条链路做成了一套完整的工业化流程。

项目采用 **Script → Asset → Keyframe(剧本 → 资产 → 关键帧)** 工作流:先完成剧情与分镜,再生成角色/场景资产,然后在 AI 工作台中制作关键帧与视频,最后统一预览和导出。

---

## 核心理念:关键帧驱动

传统 Text-to-Video 难以稳定控制角色、构图和镜头起止画面。漫剧工场借鉴动画与影视制作中的关键帧思路:

1. **先画后动** — 先生成镜头的起始帧与结束帧;
2. **插值生成** — 通过视频模型在关键帧之间生成运动过渡;
3. **资产约束** — 画面生成参考角色定妆、服装变体和场景概念图,提升角色一致性与场景连续性;
4. **流程化生产** — 每个阶段围绕同一个项目状态推进,减少反复复制提示词和手工整理资产的成本。

## 功能模块

**Phase 01 剧情创作**
- AI 智能剧本拆解:输入故事大纲,自动生成剧本结构、角色、场景和分镜
- 分镜规划:按目标时长和节奏生成镜头列表(动作、台词、画面提示词)
- 手动编辑:支持编辑角色、场景、镜头动作、台词和分镜画面提示词

**Phase 02 场景角色**
- 角色定妆、服装变体、场景概念图批量生成
- 支持上传本地参考图作为视觉资产

**Phase 03 AI 工作台**
- 镜头网格管理与工作台视图
- 关键帧生成(起始帧/结束帧),结合场景与角色上下文提升连续性
- 长镜头拆分为子镜头,细化动作节奏
- 基于关键帧的视频生成与渲染日志

**Phase 04 制片导出**
- 时间轴预览、逐段视频播放
- 渲染日志集中查看
- 关键帧、视频片段与项目资产导出

**资产管理**
- 角色 / 场景 / 关键帧 / 视频提示词统一查看与编辑
- 按角色、场景、镜头和状态搜索筛选

## 技术架构

| 层 | 选型 |
|----|------|
| 前端 | React 19 + TypeScript + Vite |
| 样式 | Tailwind CSS + 工业风界面(深色/浅色主题,支持跟随系统) |
| 存储 | 浏览器 IndexedDB + PostgreSQL(可选后端同步) |
| AI 接口 | 兼容 OpenAI 风格,支持 GitCC API / 火山引擎 / 阿里云百炼 / DeepSeek 等多服务商 |
| 桌面端 | Electron + electron-builder(Windows / macOS 安装包) |
| 部署 | Docker + Nginx |

## 快速开始

```bash
# 本地开发
npm install
npm run dev          # 前端 http://localhost:3000
npm run dev:server   # 后端 API http://localhost:3001

# 构建
npm run build

# 桌面端
npm run electron:build     # 本平台安装包
npm run electron:build:win # Windows 安装包

# Docker 部署
docker-compose up -d --build   # http://localhost:3005
```

## 使用流程

1. 配置模型 API Key(支持多家服务商);
2. Phase 01 输入故事创意,生成剧本与分镜;
3. Phase 02 生成角色定妆、服装变体和场景图;
4. Phase 03 逐个生成关键帧和视频片段;
5. Phase 04 预览并导出关键帧、视频和项目资产。

## 适合场景

- 短剧 / 漫剧前期开发
- 动态漫画与分镜预演
- 角色与场景视觉设定
- AI 视频片段批量生成
- 创意原型验证与资产整理

---

## 开源信息

- 项目主页:https://coderx.me/
- 源码仓库:https://github.com/bo961386926/manga-studio
- 开源协议:MIT

如果你觉得这个项目有帮助,欢迎 Star、提交 Issue 或 PR 参与共建。
