# 自动化测试报告

> 执行时间：2026-09-24 · 代码基线：`feat/public-model-platform` @ `9d5c246`（第 2 批完成后）
> 执行环境：macOS (darwin) · Node 22 · 本地 PostgreSQL 16（测试库 `manga_studio_test`）

## 一、结论

**全部通过，未发现回归。** 测试后代码已推送。

| # | 检查项 | 结果 | 用例数 | 耗时 |
|---|---|---|---|---|
| 1 | 服务端测试（Node test runner + 真实 Postgres） | ✅ 77 / 77 通过 | 77 | 4.6s |
| 2 | 前端类型检查（tsc --noEmit） | ✅ 无错误 | — | — |
| 3 | 前端单元测试（Vitest） | ✅ 28 / 28 通过（6 个文件） | 28 | 0.3s |
| 4 | 生产构建（vite build） | ✅ 成功 | — | 1.4s |
| 5 | docker compose 生产配置校验 | ✅ 合法；缺必需变量时正确拒绝启动（fail-fast） | — | — |

合计：**105 个自动化用例，0 失败，0 跳过，0 取消。**

## 二、服务端测试明细（15 个文件 / 77 用例）

覆盖身份、隔离、网关安全与本次新增的厂商协议：

| 测试文件 | 覆盖内容 |
|---|---|
| `auth.test.js` | 邮箱归一化、密码策略、注册/登录/会话流程、防枚举 |
| `bootstrap.test.js` | 管理员引导（维护模式、advisory lock、一次性 token） |
| `entitlements.test.js` | VIP 权益发放/撤销/过期、注册开关 |
| `routes.test.js` | 认证路由集成（CSRF、限流、会话） |
| `security.test.js` | CSRF/CORS/安全头/匿名变更守卫 |
| `isolation.test.js` | 用户数据隔离（projects/assets/settings 按 user_id 隔离） |
| `migration.test.js` | 旧配置迁移、密钥脱敏 |
| `invocation.test.js` | 网关同步调用、幂等重放不重复调用 |
| `model-gateway.test.js` | 凭据加密 AAD 绑定、密钥轮换拒绝、RLS 表清单 |
| `media.test.js` | 媒体资产上传/读取/删除 |
| `upstream-security.test.js` | SSRF 防护（私网地址、元数据端点、重定向、危险头） |
| `no-unsafe-proxy.test.js` | 旧任意转发代理已彻底移除 |
| `prod-hardening.test.js` | 生产密钥 fail-close、凭据注入上游、限流按 req.ip |
| `vendor-presets.test.js` | **本次新增**：豆包/千问/海螺异步视频协议线格式（请求体、状态分类、结果抽取）、轮询 deps 组装 |
| `bootstrap/helpers` | 测试基建（迁移、夹具、TRUNCATE） |

## 三、前端测试明细（6 个文件 / 28 用例）

| 测试文件 | 用例 | 覆盖 |
|---|---|---|
| `StageDirector.utils.test.ts` | 8 | 镜头参考图选择逻辑 |
| `StageDirector.cameraMovementGuides.test.ts` | 10 | 镜头运动提示词 |
| `auth-client.change-password.test.ts` | 3 | 改密客户端流程 |
| `gateway-client.characterization.test.ts` | 1 | 网关客户端行为基线 |
| `video-cancel.characterization.test.ts` | 2 | 视频取消流程基线 |
| `model-routing.characterization.test.ts` | 4 | 模型路由基线（含网关模型解析） |

## 四、本批新增覆盖说明

第 2 批（核心链路恢复）新增的测试保障：

- **厂商协议 fixture 测试**（7 例）：豆包 `--resolution/--duration` 后缀与 content 数组、千问 `X-DashScope-Async` 头与 `output.task_status` 状态机、海螺 v2 路径与 `task.content.url` 抽取，未知状态一律归为 waiting（防止任务被误判终止）；
- **轮询接线测试**：`buildJobPollDeps` 按 preset 组装状态查询（URL/方法）与结果下载（content-type 传递、失败态带厂商错误消息）；
- **fail-close 测试**（10 例，第 1 批引入）：4 个生产密钥缺失即拒绝工作、网关凭据注入 Authorization/api-key 头、限流键使用 req.ip。

## 五、已知限制（不阻塞提交）

1. **真实上游未打真**：厂商协议测试基于 fixture 断言（形状来自生产验证过的 geminiService 实现），未对豆包/千问/海螺真实 API 发起调用——第 3 批 staging E2E 用真实 key 完成最终验证。
2. **CI 未跑通验证**：`server-test`/pnpm 改造后的 workflow 需要一次真实 push 触发来验证（本地无 GitHub Actions 环境）。
3. Electron 44 为锁文件级升级（`--lockfile-only`），打包验证留待下次出包时进行。

## 六、提交记录

测试通过后推送以下提交至 `origin/feat/public-model-platform`（详见 git log）：

- `9d5c246` docs: mark stage-2 tasks complete
- `12f4c1b` feat(electron): remove local api-proxy shell; upgrade toolchain
- `4a42902` feat(gateway): route stage AI calls through the server gateway
- `c59de10` feat(gateway): vendor async video presets and job poll-on-read
- `23ec814` / `2bdb695` docs rescope / fix upload path + Node 22
- `97dad5a` / `cbcf649` 会员体系设计与轻商业化方案
- 及此前 8 个提交（硬化批次、身份/网关/迁移/接线）
